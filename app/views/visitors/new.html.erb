<div class="row" >
  <ul class="thumbnails">
    <% @articles.each do |article, index| %>
      <% if index%3 == 0 %>
      <%= render "/articles/thumb_article", article: article %>
    <% end %>
  </ul>
</div>
<%= paginate @articles %>
