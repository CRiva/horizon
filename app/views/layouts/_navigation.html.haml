.navbar.navbar-default.navbar-fixed-top
  .container-fluid
    .navbar-header
      = link_to image_tag('subh.png'), root_path
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        - Page.all.each do |page|
          %li.navbar-brand= link_to page.name, articles_path(id: page)
