%h2= @user.email
- if @user.name == nil
  = link_to "add name", edit_user_path
- else
  = link_to @user.name, edit_user_path
|
= link_to "Change Password", edit_user_registration_path
= render partial: "user_panel"
