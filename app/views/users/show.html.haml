= image_tag @user.avatar.url(:thumb)

%h2= @user.email
- if current_user.id == @user.id
  - if @user.name == nil
    %h4= link_to "add name", edit_user_path
  - else    
    = link_to @user.name, edit_user_path
    |
    = render partial: "user_panel"
